#include <iostream>
#include "stdio.h"
#include <math.h>
using namespace std;

int main()
{
    double r, h, s, π = 3.14;
    int V;
    double x = pow(π, 2);
    cout << "Input h" << endl;
    cin >> h;
    cout << "Input r" << endl;
    cin >> r;
    s = 2 * π * r * h;
    V = π * r * x * h;
    cout << "Result s =" << s << endl;
    cout << "Result V =" << V << endl;

